#!/bin/bash
# DISABLED — cloudflared quick tunnels rate-limited by cloudflare after repeated restarts
# Re-enable only when: a) cloudflared auth token provided, or b) this runs on a VPS that never sleeps
# Date disabled: 2026-05-06 10:12 CEST
exit 0
